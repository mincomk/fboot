# fboot
Manage your servers in a simple dashboard. fboot uses IPMI to control your servers and communicate via serial console. And it hosts ProxyDHCP, TFTP, HTTP server combo to boot your servers to your desired OS without manually plugging in USB drives. You can simply click a button to select whether your servers should boot in a network-initiated environment or boot from the local disk.
